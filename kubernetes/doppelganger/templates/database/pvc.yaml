kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: database-disk
  labels:
    app: database
    stage: production
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
